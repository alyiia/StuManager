#ifndef UI_H
#define UI_H
#include <iostream>
#include <string>
#include "List.h"

void mainUI();
void sureUI(bool& yes);
void addUI(Node* list);
void delUI(Node* list);
void modUI(Node* list);

#endif